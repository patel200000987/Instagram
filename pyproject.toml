[project]
name = "ig-reel-api"
version = "1.0.0"
description = "Instagram Reel Downloader API"
requires-python = ">=3.9"
dependencies = [
    "flask==2.3.3",
    "requests==2.31.0"
]

[tool.vercel]
entrypoint = "api/index.py:app"
