[project]
name = "ig-downloader-api"
version = "1.0.0"
description = "Instagram Reel Downloader API using yt-dlp"
requires-python = ">=3.9"
dependencies = []

[tool.vercel]
entrypoint = "api/download.py:handler"
